import * as React from 'react';

interface Props {
    image: string,
    letter: string
}

const FrontCard: React.FC<Props> = ({ image, letter }) => {
    return (

    );
}

export default FrontCard;