import * as React from 'react';

interface Props {
    question: string,
    answer: string
}

const BackCard: React.FC<Props> = ({ question, answer }) => {
    return (

    );
}

export default BackCard;